export interface FormTextareaProps {
  placeholder?: string;
  disabled?: boolean;
  required?: boolean;
  rows?: number;
  maxlength?: number;
  resize?: "none" | "both" | "horizontal" | "vertical";
}
